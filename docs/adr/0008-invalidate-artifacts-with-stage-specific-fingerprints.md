# 用阶段输入指纹使旧制品失效

PaperRadar 不实现一个主观的“论文是否发生实质变化”判断，而为筛选、参考文献处理和全文精读分别保存规范输入指纹。只有进入该阶段语义输入的事实发生变化时，既有建议或分析才过期；卷期页码、普通 URL 等书目补充不会触发无意义的模型重跑。旧人工筛选决定即使基于旧版本仍保持最高执行优先级，同时明确标记并提示复核；旧系统决定则由基于新输入的建议替换。这个选择需要分别维护任务资格和过期原因，但避免一次小型元数据更新重跑整条管线，也不会让 Agent 静默推翻人工决定。
