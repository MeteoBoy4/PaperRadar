# 只自动下载无需用户会话的开放 PDF

PaperRadar 第一版只自动下载出版者、合法仓储或 Unpaywall 明确提供且无需登录、验证码或用户会话的开放 PDF。Cloudflare 或其他 `403` 记录为访问受阻而不是非开放获取，并把论文送入人工 PDF 队列；系统不绕过访问控制，也不复用浏览器登录态。第二版可以在用户明确控制下重新评估浏览器辅助下载。这个边界会增加人工附加 PDF 的数量，但使自动运行的法律、凭据和站点兼容风险保持可控。
